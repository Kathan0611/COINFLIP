
// Define the data types for form inputs and coin images
export type FormData = {
    user_name: string;
    mobile_number: string;
    prediction_value: "head" | "tail";
  };

 export  type CoinImages = {
    head: string;
    tail: string;
  };